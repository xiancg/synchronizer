language: python
python:
    - "3.7"
install:
    - pip install tox
    - pip install pipenv
    - pipenv install
    - pipenv install --dev
script:
    - tox -e flake8
    - tox -e py27
    - tox -e py37
after_success:
    - coveralls